{
  "name": "simple-mm-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "MODE=live bun run src/main.ts",
    "dev:paper": "MODE=paper bun run src/main.ts",
    "dev:backtest": "CONFIG_PATH=config/config.backtest.yml MODE=backtest bun run src/main.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "check": "vp check",
    "check:fix": "vp check --fix",
    "lint": "bun run lint:code",
    "lint:code": "vp lint",
    "lint:fix": "vp lint --fix",
    "format": "vp fmt",
    "format:check": "vp fmt --check",
    "loop:backtest-paper": "bun run scripts/backtestPaperLoop.ts",
    "metrics:evaluate": "bun run scripts/evaluateLiveRun.ts",
    "metrics:tune": "bun run scripts/tuneBulkConfig.ts",
    "metrics:issues": "bun run scripts/createDesignIssues.ts",
    "metrics:report": "bun run scripts/generateMetricsReport.ts",
    "report:generate": "bun run scripts/generateReport.ts",
    "test": "bun run test:unit && bun run test:integration",
    "test:unit": "bun test tests/domain tests/scripts tests/application tests/adapters tests/reporting tests/config.test.ts tests/package.test.ts",
    "test:integration": "bun test tests/infrastructure",
    "test:e2e:paper": "bun test tests/e2e"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "@nktkas/hyperliquid": "^0.32.2",
    "@t3-oss/env-core": "^0.13.11",
    "bulk-ts-sdk": "^0.0.10",
    "drizzle-orm": "^0.44.7",
    "neverthrow": "^8.2.0",
    "postgres": "^3.4.9",
    "viem": "^2.48.8",
    "yaml": "^2.8.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.9.3",
    "vite-plus": "^0.1.20"
  }
}
